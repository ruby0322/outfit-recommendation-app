// item.ts
const getItemById = async (item_id: string): Promise<Item> => {
    /* TODO: Retrieve an item by its ID. */
};

const getItemsByIds = async (item_ids: string[]): Promise<Item[]> => {
    /* TODO: Retrieve multiple items by their IDs. */
};

export { getItemById, getItemsByIds };